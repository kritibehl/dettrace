# timeout_chain
Simulates a cascading timeout where one delayed dependency causes later stages to diverge.
Expected use: compare healthy vs degraded traces and isolate first timing-sensitive divergence.
