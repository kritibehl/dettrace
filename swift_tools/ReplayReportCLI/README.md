# ReplayReportCLI

Small Swift CLI example for parsing DetTrace replay report summaries.

This is a lightweight Swift/XCTest-style proof artifact. It does not claim production Apple tooling or Xcode project ownership.

## Run

```bash
swift swift_tools/ReplayReportCLI/ReplayReportCLI.swift device_replay/reports/device_replay_summary.json
Test example

Tests/ReplayReportParserTests.swift shows an XCTest-style unit test for replay report fields.
