# DetTrace Terraform Deployment Skeleton

This directory documents a minimal deployment shape for DetTrace.

It is intentionally lightweight and does not claim production-scale infrastructure.

Use this as IaC proof for:
- containerized service deployment
- configurable service port
- environment-aware deployment variables

Commands:

```bash
terraform init
terraform plan
